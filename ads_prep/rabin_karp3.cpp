#include <iostream>
using namespace std;

const long long mod = 1e9 + 7;
const prime = 31;

vector<long long> prefix_hash(string my_string){
    int n = my_string.size();
    vector<long long> hashh(n + 5, 0);
    vector<long long> prime(n + 5, 1);

    for(int i = 0; i < n; i++){
        
    }
}

int main(){

    

    return 0;
}